/* Funciones Flecha */
var colores = ['blanco', 'negro', 'azul']

colores.forEach(function(color){
    console.log(color)
});

colores.forEach((color) => {
    console.log(color)
});

colores.forEach(color => {
    console.log(color)
});